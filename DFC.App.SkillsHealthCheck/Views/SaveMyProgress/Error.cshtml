@model DFC.App.SkillsHealthCheck.ViewModels.SaveMyProgress.ErrorDocumentViewModel

@{
    Layout = "_LocalLayout";

    if (!string.IsNullOrWhiteSpace(Model.HeadViewModel?.Title))
    {
        ViewData["Title"] = Model.HeadViewModel.Title;
    }
}
<partial name="Breadcrumb" model=@Model.BreadcrumbViewModel />

<div class="govuk-grid-row">
    <div class="govuk-grid-column-full">
        <partial name="ErrorBody" model="@Model.BodyViewModel" />
    </div>
</div>