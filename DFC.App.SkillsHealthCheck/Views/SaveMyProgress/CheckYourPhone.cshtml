@model DFC.App.SkillsHealthCheck.ViewModels.SaveMyProgress.GetCodeViewModel

@{
    Layout = "_LocalLayout";

    if (!string.IsNullOrWhiteSpace(Model.HeadViewModel?.Title))
    {
        ViewData["Title"] = Model.HeadViewModel.Title;
    }
}
<partial name="Breadcrumb" model=@Model.BreadcrumbViewModel />

<div class="govuk-grid-row">
    <div class="govuk-grid-column-full">
        <partial name="CheckYourPhoneBody" model="@Model.BodyViewModel" />
    </div>
</div>